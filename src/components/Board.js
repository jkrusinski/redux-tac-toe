import React from 'react';

const styles = {
  board: {
    width: '300px',
    height: '300px',
    backgroundColor: 'lightblue',
    borderRadius: '5px'
  }
}

const Board = () => (
  <div style={styles.board}></div>
);

export default Board;
