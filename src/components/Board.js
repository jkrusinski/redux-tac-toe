import React from 'react';

const styles = {
  board: {
    width: '200px',
    height: '200px',
    backgroundColor: 'lightblue'
  }
}

const Board = () => (
  <div style={styles.board}></div>
);

export default Board;
